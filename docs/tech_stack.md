# 🛠️ AniMatch — 기술 스택 최종 확정

> **버전**: v1.0
> **확정일**: 2026-02-16
> **기준**: 사이드 프로젝트 규모, 1인 개발, 최소 비용

---

## 아키텍처 개요

```
┌─────────────────────────────────────────────────────────┐
│  Client (Browser)                                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ React + Vite │  │ CSS Modules  │  │ ONNX Runtime │  │
│  │ + TypeScript  │  │              │  │ (WASM)       │  │
│  └──────┬───────┘  └──────────────┘  └──────┬───────┘  │
│         │                                    │          │
│         │ API 요청                           │ 클라이언트│
│         │                                    │ 사이드    │
│         ▼                                    │ 추론      │
│  ┌──────────────────────────────────┐        │          │
│  │ Cloudflare Workers (API + ML)   │◄───────┘          │
│  │  ├── D1 (캐릭터 DB)             │                    │
│  │  ├── R2 (이미지/모델 저장)       │                    │
│  │  └── KV (캐시, 세션)            │                    │
│  └──────────────────────────────────┘                    │
│                                                          │
│  Cloudflare Pages ← 프론트엔드 호스팅                     │
└─────────────────────────────────────────────────────────┘
```

---

## 계층별 기술 선택

### 프론트엔드

| 항목 | 선택 | 대안 (불채택) | 선택 사유 |
|------|------|-------------|-----------|
| 프레임워크 | **React 19 + Vite 6** | Next.js 15 | SSR 불필요 (SPA 충분), 빌드 속도, 단순성 |
| 언어 | **TypeScript 5.x** | JavaScript | 타입 안정성, 리팩토링 안전성 |
| 스타일링 | **CSS Modules** | Tailwind, Styled-components | 스코프 격리, 프로토타입 CSS 재활용, 번들 최소화 |
| 애니메이션 | **Framer Motion** | GSAP, CSS 전용 | 선언적 API, React 통합, 가챠 연출에 적합 |
| 라우팅 | **React Router v7** | — | SPA 라우팅 표준 |
| 상태관리 | **Zustand** | Redux, Jotai | 러닝커브 최소, 보일러플레이트 없음 |
| 이미지 크롭 | **react-image-crop** | Cropper.js | React 네이티브 통합, 터치 지원 |

### 백엔드

| 항목 | 선택 | 대안 (불채택) | 선택 사유 |
|------|------|-------------|-----------|
| API 서버 | **Cloudflare Workers** | AWS Lambda, Vercel | 글로벌 엣지, 콜드 스타트 없음, 무료 티어 충분 |
| 데이터베이스 | **Cloudflare D1** | Supabase, PlanetScale | Workers 네이티브, SQLite 기반, 무료 |
| 파일 저장 | **Cloudflare R2** | S3, Supabase Storage | egress 무료, Workers 네이티브 |
| 캐시 | **Cloudflare KV** | Redis | Workers 네이티브, 설정 간단 |
| API 프레임워크 | **Hono** | itty-router | TypeScript 지원, 미들웨어 풍부 |

### ML / AI

| 항목 | 선택 | 대안 (불채택) | 선택 사유 |
|------|------|-------------|-----------|
| 얼굴 감지 | **MediaPipe Face Detection** | dlib, MTCNN | 브라우저 WASM 지원, 경량 |
| 얼굴 임베딩 | **InsightFace (ArcFace)** | FaceNet, DeepFace | ONNX 지원, 높은 정확도, 상업 가능 |
| 실사→애니 변환 | **AnimeGAN v2** | U-GAT-IT, CartoonGAN | 경량, ONNX 변환 가능, 품질 양호 |
| 추론 런타임 | **ONNX Runtime (WASM)** | TensorFlow.js, Workers AI | 크로스 플랫폼, 모델 호환성 높음 |
| 모델 학습 | **PyTorch → ONNX 변환** | TensorFlow | 연구 커뮤니티 표준, 변환 생태계 성숙 |

### 인프라 / 배포

| 항목 | 선택 |
|------|------|
| 프론트엔드 호스팅 | Cloudflare Pages |
| 도메인 / DNS / SSL | Cloudflare (통합) |
| CI/CD | GitHub Actions |
| 모니터링 | Cloudflare Analytics + Sentry |
| 에러 트래킹 | Sentry (무료 티어) |

### 광고 / 분석

| 항목 | 선택 |
|------|------|
| 광고 | Google AdSense |
| 분석 | Google Analytics 4 + Cloudflare Web Analytics |
| OG 이미지 | @vercel/og 또는 Satori (Workers 호환) |

---

## 비용 추정 (월)

| 항목 | 무료 티어 한도 | 예상 사용량 (초기) | 비용 |
|------|-------------|------------------|------|
| Workers | 100K req/일 | ~10K req/일 | $0 |
| D1 | 5M rows read/일 | ~50K rows/일 | $0 |
| R2 | 10GB 저장, 10M req/월 | ~2GB, ~100K req | $0 |
| KV | 100K reads/일 | ~10K reads/일 | $0 |
| Pages | 무제한 | — | $0 |
| Sentry | 5K events/월 | ~1K events | $0 |
| **합계** | | | **$0** |

> DAU 10,000+ 이후 Workers Pro ($5/월) 전환 검토

---

## 개발 환경

| 항목 | 도구 |
|------|------|
| 패키지 매니저 | pnpm |
| Linter | ESLint + Prettier |
| 테스트 | Vitest + Playwright (E2E) |
| Git 전략 | GitHub Flow (main + feature branches) |
| Node.js | v20 LTS |

---

## 프로젝트 구조 (예정)

```
animatch/
├── src/
│   ├── components/          # React 컴포넌트
│   │   ├── landing/
│   │   ├── upload/
│   │   ├── loading/
│   │   ├── result/
│   │   └── shared/
│   ├── hooks/               # 커스텀 훅
│   ├── stores/              # Zustand 스토어
│   ├── utils/               # 유틸리티
│   ├── ml/                  # ML 관련 (ONNX 추론)
│   ├── styles/              # 글로벌 CSS + 변수
│   ├── types/               # TypeScript 타입
│   ├── App.tsx
│   └── main.tsx
├── worker/                  # Cloudflare Workers
│   ├── src/
│   │   ├── routes/
│   │   ├── db/
│   │   └── ml/
│   └── wrangler.toml
├── public/
├── docs/                    # 프로젝트 문서
├── models/                  # ONNX 모델 (gitignore)
├── scripts/                 # 유틸리티 스크립트
├── vite.config.ts
├── tsconfig.json
└── package.json
```
